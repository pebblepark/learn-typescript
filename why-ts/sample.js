// @ts-check

/**
 *
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 * @returns number
 */
// function sum(a, b) {
//   return a + b;
// }

// 자바스크립트를 타입스크립트처럼 썼을 때
// 추론 타입을 명시할 수는 있어도 에러를 잡아주지는 못함
// @ts-check -> 적용 시 에러를 잡아줄 수 있음
// sum(10, "20");

// var result = sum(10, 20);
// result.toLocaleString();
